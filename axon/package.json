{
  "name": "@nervous-system/axon",
  "version": "0.1.0",
  "description": "AXON — the durable task ledger for the NERVous System. Survives compaction, restart, interruption, and subprocess agents. The single source of truth for multi-agent work state.",
  "keywords": [
    "pi-package",
    "nervous-system",
    "axon",
    "multi-agent",
    "task-ledger"
  ],
  "license": "MIT",
  "pi": {
    "extensions": [
      "extension/index.ts"
    ],
    "skills": [
      "skills/axon"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "@types/node": "^22.0.0",
    "typebox": "*",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
