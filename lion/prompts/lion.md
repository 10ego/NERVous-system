---
description: Delegate one concrete assignment to a LION coding subagent
argument-hint: "<objective>"
---
Delegate the following objective to one isolated LION worker:

$@

Use the `lion` tool with action `run`. Provide a narrow objective, concise context/acceptance criteria, and an AXON `task_id` if one is relevant. After the worker returns, read its WORKER_REPORT and summarize the outcome, changed files, tests run, blockers, and next steps.
